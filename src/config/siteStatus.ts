// CMS PLACEHOLDER: These two values are the single source of truth for the site status badges.
// A future CMS can expose them as simple editable toggle fields.
export const siteStatus = {
  currentlyBidding: false,
  nowHiring: true,
} as const;
